[package]
name        = "write_flux"
version     = "0.1.30"
keywords    = ["write_flux", "influxdb", "influxdb2", "reqwest", "rustls", "aws-lc-rs"]
description = "Influxdb2 point client"
edition     = "2024"
license     = "MIT OR Apache-2.0"
authors     = [ "B3K7" ]

[dependencies]
chrono              = "0.4"
clap                = { version = "4.6", features = ["derive"] }
clap-verbosity-flag = "2.2"
env_logger          = "0.11"
log                 = "0.4"
serde               = { version = "1.0", features = ["derive"] }
serde_json          = "1.0"
tokio               = { version = "1.52", features = ["full"] }

# reqwest 0.12+ (including 0.13) natively uses rustls and defaults to the aws-lc-rs crypto provider.
# We disable default features to strip out native-tls (OpenSSL).
reqwest             = { version = "0.13", default-features = false, features = ["rustls-tls"] }

[profile.release]
strip = "symbols"
opt-level = "3"
lto = "fat"
codegen-units = 1
target-cpu = "native"
panic = "abort"
